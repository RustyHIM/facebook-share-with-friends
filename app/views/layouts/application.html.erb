<!DOCTYPE html>
<html>
<head>
  <title>Facebook</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
	
	
</head>
<body>
	<big><big><big><div id="user_nav">
		<% if current_user %>
		What's up  <strong><%= current_user.name %></strong>! Not you?
		(<%= link_to "signout", signout_path, id: "sign_out" %>)
		<% else %>
		<%= link_to "CONNECT TO FACEBOOK", "/auth/facebook", id: "sign_in" %> to send Lokalty gifts/points to friends
		<% end %>
	</div>
	</big></big></big>

<%= yield %>

</body>
</html>
